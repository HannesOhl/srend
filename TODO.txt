srend:
	- dont define SCREEN_WIDTH etc thrice rend/backend/text
	- translate vertices, not triangles 		(!!!)
	- cleaner mesh type, instead of AssetModel	(!!)
	- make use of multi-threading	    		(!)
	- handle delta-time, ie. constant framerate ?	(!)


pngToBin:
	- take a look at deflate and write it on my own	(!)
	- fix the check.py to read last two bits
	  and set width/height
	-

objToC:
	- make it generally safer, allowing different	(!!)
	  face-layouts
	- support arbitrary number of verts per face 	(!)

general:
	- add Makefile with asset compilation option 	(!!!)


git push pngToBin-remote pngToBin-branch:main
git push   objToC-remote   objToC-branch:main
